services:
  - type: web
    name: my-ai-chatbot
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: MULEROUTER_API_KEY
        sync: false  # You'll set this manually in the dashboard
